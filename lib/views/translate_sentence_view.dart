import 'package:flutter/material.dart';

class TranslateSentenceView extends StatelessWidget {
  const TranslateSentenceView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('This is the translate sentence exercise'),
      ),
      body: const Text('Heeeeeelllp'),
    );
  }
}
